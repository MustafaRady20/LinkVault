# LinkVault (work in progress )

A RESTful API service where users can save, organize, and search their bookmarks (URLs) with tags, collections, and metadata auto-fetching.

Tech Stack:

Chi — sqlc — golang-migrate — pgx — Docker + docker-compose — Makefile — testify — zerolog — viper — air — 